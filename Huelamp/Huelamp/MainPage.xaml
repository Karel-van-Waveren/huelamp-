<Page
    x:Class="Huelamp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Huelamp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Huelamp.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:DataType="data:Huelampwaardes" x:Key="textTemplate">
            <StackPanel Tapped="Infolamp_Tapped">
                <Rectangle Height="50" Width="50" Fill="Red" Margin="0,5,0,5"/>
                <TextBlock Text="{x:Bind naam}"/>
                <StackPanel Visibility="Visible">
                    <TextBlock Text="On" Margin="0,15,0,0"/>
                    <ToggleSwitch IsOn="{x:Bind on}"/>
                    <TextBlock Text="Hue" Margin="0,15,0,0"/>
                    <Slider Maximum="65555" Value="{x:Bind hue}"/>
                    <TextBlock Text="Saturation" Margin="0,15,0,0"/>
                    <Slider Maximum="255" Value="{x:Bind saturation}"/>
                    <TextBlock Text="Brightness" Margin="0,15,0,0"/>
                    <Slider Maximum="255" Value="{x:Bind brightness}"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>


    <ListView ItemsSource="{x:Bind huelampen}" ItemTemplate="{StaticResource textTemplate}">

    </ListView>
</Page>
